#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() 
{
	// onluk tabanda yazıldan sayıyı ikilik tabana çevirme ,
	/*
	int sayi,ikilik,i;
	printf(" sayi giriniz : "); 
	scanf("%d",&sayi);
	for (i = 8; i>=0;i--)                      // --> i her dongude azalıyor
	{
			ikilik = sayi >> i;				  // --> >> = bir karakter kaydır -örn- 		x >> y
			if (ikilik & 1)
			printf(" 1 ");
			else 
			printf(" 0 ");
	}
	*/
	
	
	// hipotenüs hesaplama
						// -- > gerekli kütüphane #include <math.h>
	/*			
	int k1,k2;			// -- > integer değerler atadık
	float hipo;			// -- > flaot degerler atadık
	printf(" iki dik kenar giriniz: ");		// --> kullanıcıdan iki tane dik kenar verisi aldık 
	scanf(" %d%d ",&k1,&k2);			
	
	hipo = sqrt(( k1*k1)+ (k2*k2));
	
	printf(" hipoteus : %.2f",hipo);
	return 0;
	*/
	
	 
}	
